#pragma once
#include "Material.h"

const buki::Material& buki::Material::Rock = Material(0.6f, 0.1f);
const buki::Material& buki::Material::Wood = Material(0.3f, 0.2f);
const buki::Material& buki::Material::Metal = Material(1.2f, 0.05f);
const buki::Material& buki::Material::SuperBall = Material(0.3f, 0.95f);
//const buki::Material& buki::Material::SuperBall = Material(1.0f, 0.2f);
const buki::Material& buki::Material::Pillow = Material(0.1f, 0.2f);
const buki::Material& buki::Material::Static = Material(0.0f, 0.4f);